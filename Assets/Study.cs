using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class Study : MonoBehaviour
{
    int[] tArray = new int[30];//30칸짜리 배열을 생성
    List<int> tList = new List<int>();//소괄호를 넣어 줘야 한다. 꺽쇄 안에 int형의 자료만 쓴다고 정해준 것이다.
    Dictionary<string, Monster> tDic;//딕셔너리는 키와 값이 있고 이 키로 값을 꺼내는거고 이때 키는 문자열로 하겠다는 뜻이다
    Dictionary<int, int> gachaRateDic;
    Dictionary<>

   
}


//리스트는 순번만 있다.
//csv리더는 순번으로 하기 때문에 딕셔너리로 아이디를 검색하게 한다.